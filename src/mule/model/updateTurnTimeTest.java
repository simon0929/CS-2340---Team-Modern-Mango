package mule.model;

import static org.junit.Assert.*;

import org.junit.Test;

public class updateTurnTimeTest {

	//@Test
	/*public void test() {
		Player player = new Player("Tyler","Human", javafx.scene.paint.Color.BLUE, "Beginner");
		player.setFood(5);
		player.setEnergy(5);
		player.setOre(5);
		player.setMoney(0);

		
		player.sellResource("food");
		assertEquals(4, player.getFood());
		int playerMoney = player.getMoney();
		int storePrice = Store.getResourceList().get("food");
		assertEquals(playerMoney, storePrice);
		
		player.sellResource("ore");
		assertEquals(4, player.getOre());
		storePrice = Store.getResourceList().get("ore");
		playerMoney += storePrice;
		assertEquals(playerMoney, player.getMoney());
		
		player.sellResource("energy");
		assertEquals(4, player.getEnergy());
		storePrice = Store.getResourceList().get("energy");
		playerMoney += storePrice;
		assertEquals(playerMoney, player.getMoney());
		
		player.setFood(0);
		player.setOre(0);
		player.setEnergy(0);
		
		playerMoney = player.getMoney();
		player.sellResource("food");
		assertEquals(0, player.getFood());
		storePrice = Store.getResourceList().get("food");
		assertEquals(playerMoney, player.getMoney());
		
		player.sellResource("ore");
		assertEquals(0, player.getOre());
		storePrice = Store.getResourceList().get("ore");
		assertEquals(playerMoney, player.getMoney());
		
		player.sellResource("energy");
		assertEquals(0, player.getEnergy());
		storePrice = Store.getResourceList().get("energy");
		assertEquals(playerMoney, player.getMoney());
		
		
	}*/

}
